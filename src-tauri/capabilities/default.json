{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Both app windows: custom commands plus core event listening",
  "windows": ["popup", "settings"],
  "permissions": ["core:default"]
}
